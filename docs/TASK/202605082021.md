# Current Task

- Summary: Typing 画面の `Target Text` ブロックで、出題本文の上下に2行ずつ空行を表示する。
- Docs:
  - Done: PLAN、REQUIREMENTS、SPECIFICATIONS、DESIGN、ADR を `Target Text` ブロック余白変更に同期する。
- Implementation:
  - Done: `src/presentation/ui/render/typing.rs` の `Target Text` ブロック描画を、本文前2行、本文、本文後2行の行構成にする。
  - Done: 出題本文が複数行に折り返されても本文後2行の空行が維持されるようにする。
  - Done: WPM グラフ表示時も `Target Text` ブロックが最低8行を確保し、本文2行と上下2行の空行を表示できるようにする。
  - Done: 既存の入力判定、WPM グラフ、ヘッダー、フッター、カーソル形状、カーソル位置の意味を変更しない。
- Verification:
  - Done: `make check` を実行する。
  - Done: `make build` を実行する。
