# Current Task

- Date: 2026-04-09 22:38
- Summary: タイムアップなしの練習モード、strict 判定、`Esc` によるメニュー復帰を既存フローへ追加する。
- Scope:
  - Update requirements, ADR, specifications, and design for practice mode and strict typing behavior.
  - Define `-t 0` / `--timeout 0` as a no-timeout practice mode.
  - Define strict typing so incorrect input does not advance the target position.
  - Define `Esc` behavior in `Typing` as returning to `Menu`.
  - Keep result-screen accuracy wording unchanged unless needed by the new input flow.
- Verification:
  - Confirm `timeout=0` sessions do not finish by timeout.
  - Confirm incorrect key presses do not advance the typing cursor.
  - Confirm `Esc` during `Typing` returns to `Menu`.
  - Confirm existing result metrics still render consistently after the input-flow change.
