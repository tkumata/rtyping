# Current Task

- Summary: WPM Trend ブロックの枠線だけを薄い黄色にする。
- Docs:
  - Done: PLAN、REQUIREMENTS、SPECIFICATIONS、DESIGN、ADR を WPM Trend ブロック枠線色変更に同期する。
- Implementation:
  - Done: `src/presentation/ui/render/wpm_graph.rs` の WPM グラフ用 `Block` 枠線色だけを薄い黄色へ変更する。
  - Done: グラフ本体の緑系線色、高値部分のオレンジ強調、WPM 履歴、レイアウト、入力判定、画面遷移は変更しない。
  - Done: README.md に WPM Trend ブロックの枠線色仕様を反映する。
- Verification:
  - Done: `make check` を実行する。
  - Done: `make build` を実行する。
